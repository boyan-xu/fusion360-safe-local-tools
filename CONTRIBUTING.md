# 贡献说明

欢迎改进文档、兼容性及可恢复工作流。请在 PR 中说明问题、变化和验证方法。

1. 先运行 README 中的单元测试。新增涉及权限、计数或恢复的行为应补充不变量测试。
2. 单元测试通过不能替代 Fusion 实机验证；注明已测系统/Fusion 版本、实际操作和未验证范围。
3. 测试只用明确允许的独立设计。不要自动修改已有模型、上传数据、调用 AI 或取消大小限制。
4. 不得把失败文件记为成功，不得跳过未完成规格，不得覆盖用户文件。
5. 提交前排除设计文件、个人路径、密钥、账号、截图和对话账本。使用虚构数据描述问题。
6. 不要把 `confirmation` / `evidence` 当作真实性校验或安全沙箱；记录必须来自实际授权和验证。

本项目采用 MIT 许可证。提交代码前确认你有权以该许可证提供相关内容。Fusion 本地插件与 Codex 技能可分别使用；更改一方时说明另一方的兼容性影响。
